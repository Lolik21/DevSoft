<UserControl x:Class="Gwent.Card"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Gwent"
             mc:Ignorable="d" 
             d:DesignHeight="110" d:DesignWidth="80" MinWidth="80">
    <Grid x:Name="CardGrid" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Image x:Name="imgCardImage" Source="D:\24a.png"/>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="8*"/>
                <ColumnDefinition Width="47*"/>
                <ColumnDefinition Width="161*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="7*"/>
                <RowDefinition Height="48*"/>
                <RowDefinition Height="245*"/>
            </Grid.RowDefinitions>
            <Ellipse x:Name="Ellipse" Fill="White" Grid.Column="1" Grid.Row="1"/>
            <TextBlock x:Name="tbCardPower" Grid.Column="1" Grid.Row="1" Text="5" FontSize="18" FontFamily="Times New Roman" Foreground="Black" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Grid>


    </Grid>
</UserControl>
