#ifndef MYSCENE_H
#define MYSCENE_H

#include <QGraphicsScene>
#include <myline.h>


class MyScene : QGraphicsView
{
    Q_OBJECT
public:
    MyScene();
private slots:
    void mousePressEvent(QMouseEvent *event);
    void mouseMoveEvent(QMouseEvent *event);
    void mouseReleaseEvent(QMouseEvent *event);
protected:
    QGraphicsScene *Scene;
    bool IsKeyPressed;
    MyLine *CurrentLine;
    
        
    
    
};

#endif // MYSCENE_H